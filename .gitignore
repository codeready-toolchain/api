# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.idea/
/vendor
/out
build/_output
# the 'deploy' directory should not be committed: all generated CRD files should be dispatched in the 'host-operator' and 'member-operator' repositories
deploy
